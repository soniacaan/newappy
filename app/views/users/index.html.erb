<% provide(:title, 'All users') %>
<div class="container">
<div class="row">
<h1>All users</h1>

<ul class="users">

  	<%= render @users %>
</ul>

<%= will_paginate @users %>

<%= link_to 'New User', new_user_path %>


</div>
</div>